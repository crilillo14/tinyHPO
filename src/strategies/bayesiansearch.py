# reduced grid search with bayesian optim

from utils.gaussian_process import gaussian_process
from tinygrad import tensor, nn, dtypes

def bayesian_search(parameter_space,
                   ):
    pass
